// @flow

import React, { PureComponent } from "react";
import PropTypes from "prop-types";
import { Text } from "react-native";

type Props = {};
type State = {};

class Trails extends PureComponent<Props, State> {
  render() {
    return <Text>Trails</Text>;
  }
}

export default Trails;
