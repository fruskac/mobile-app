/**
 * @flow
 */
import { StyleSheet, Platform } from "react-native";
import { textColor } from "../../styles/CommonStyles";

export default StyleSheet.create({
  buttonHolder: { flexDirection: "row", justifyContent: "flex-start" }
});
