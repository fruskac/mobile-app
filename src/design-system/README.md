# Design System Scaffolding

This is a placeholder for the upcoming design system integration.

## Plan
- Centralize tokens (colors, typography, spacing) in a single theme entry point.
- Gradually move component styles to use tokens.
- Keep existing visual design until a dedicated redesign phase.

## Current Entry Point
`src/design-system/theme.js` is the single theme module. It should be used by all shared styles.
