import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  buttonHolder: {
    marginLeft: 12,
    flexDirection: 'row',
    justifyContent: 'flex-start',
    alignItems: 'center'
  },
  icon: {
    color: 'black'
  }
});
